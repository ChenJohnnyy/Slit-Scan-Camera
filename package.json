{
  "name": "slit-scan-camera",
  "version": "1.0.0",
  "description": "A web application for live slit-scan photography",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css app.js _redirects _headers dist/",
    "clean": "rm -rf dist",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "private": true,
  "engines": {
    "node": ">=16.0.0"
  }
} 